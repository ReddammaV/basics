import { HostelementDirective } from './hostelement.directive';

describe('HostelementDirective', () => {
  it('should create an instance', () => {
    const directive = new HostelementDirective();
    expect(directive).toBeTruthy();
  });
});
