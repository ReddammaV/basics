import { WelcomeuppercasePipe } from './welcomeuppercase.pipe';

describe('WelcomeuppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new WelcomeuppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
